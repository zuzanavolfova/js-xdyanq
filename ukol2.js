let jmeno = prompt('Jaké je tvé jméno');
let vek = prompt('Kolik ti je let');

If (vek>18) {
console.log('Vaše jméno je ' + jmeno + ', je Vám ' + vek + 'let a tak mate pravo ridit auto');
}
else { console.log( "Vaše jméno je" + jmeno + ", je Vám " + vek + "a tak musíte ještě počkat."  )}
